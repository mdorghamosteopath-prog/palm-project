import React,{useMemo,useState}from'react';
import{Users,Heart,Camera,ChevronRight,Check,X,LockKeyhole,Sparkles}from'lucide-react';
import{buildCompatibility,shareCompatibility,downloadShareCard}from'./social.js';
const first=n=>n?.replace(/^Profil\s+/,'Ami ')||'Profil';
const DUO=[['VÉRITÉ','Quelle est la première chose que tu as vraiment remarquée chez l’autre ?'],['PRÉDICTION','Qui craquerait le premier après 24 h sans téléphone ?'],['VÉRITÉ','Quel truc chez l’autre t’agace… mais te ferait bizarre s’il disparaissait ?'],['DÉFI','Regardez-vous 10 secondes sans rire. Le premier qui craque a perdu.'],['CHOIX','Qui survivrait le mieux à un voyage improvisé sans aucun plan ?'],['VÉRITÉ','Sur quoi l’autre te comprend mieux que la plupart des gens ?'],['DÉFI','Imite une habitude de l’autre. Il/elle doit deviner laquelle.'],['PRÉDICTION','Qui est le plus susceptible d’envoyer un message puis de le regretter ?']];
const GROUP=[['VOTE SECRET','Qui dans le groupe serait le meilleur pour garder un énorme secret ?'],['VOTE','Qui survivrait le plus longtemps dans une émission de télé-réalité ?'],['ACTION','Tout le monde pointe la personne la plus susceptible de partir sur un coup de tête. 3… 2… 1…'],['VÉRITÉ','Qui donne la meilleure première impression… et qui change le plus quand on le connaît ?'],['VOTE','Qui pourrait convaincre tout le groupe de faire une mauvaise idée ?'],['ACTION','Chacun donne en un mot le superpouvoir social de la personne à sa droite.'],['VOTE','Qui répondrait présent à 3 h du matin sans poser de question ?'],['DÉBAT','Vous avez 20 secondes : qui serait le meilleur leader du groupe sur une île déserte ?']];
export default function SocialHub({history,onScan,onOpen,onRename,onDelete,unlocked=false,onUnlock}){const[mode,setMode]=useState('hub'),[selected,setSelected]=useState([]),[card,setCard]=useState(null);const toggle=id=>setSelected(s=>s.includes(id)?s.filter(x=>x!==id):[...s,id]);const people=history.filter(h=>selected.includes(h.id));const match=mode==='matchResult'&&people.length===2?buildCompatibility(people[0],people[1]):null;const randomCard=list=>setCard(list[Math.floor(Math.random()*list.length)]);const group=useMemo(()=>{if(people.length<3)return null;const pairs=[];for(let i=0;i<people.length;i++)for(let j=i+1;j<people.length;j++){const c=buildCompatibility(people[i],people[j]);pairs.push({a:people[i],b:people[j],c})}pairs.sort((a,b)=>b.c.score-a.c.score);const avg=Math.round(pairs.reduce((s,p)=>s+p.c.score,0)/pairs.length);return{pairs,avg,leader:[...people].sort((a,b)=>b.scores[1]-a.scores[1])[0],connector:[...people].sort((a,b)=>b.scores[0]-a.scores[0])[0],wild:[...people].sort((a,b)=>b.scores[2]-a.scores[2])[0]}},[people]);
const paywall=(title,copy)=><div className="socialPaywall"><LockKeyhole/><span>PALM UNLIMITED</span><h3>{title}</h3><p>{copy}</p><button className="primary" onClick={onUnlock}>DÉBLOQUER TOUT — 1,99 €</button><small>Paiement unique • accès illimité</small></div>;const game=(title,sub,list)=><section><button className="backLink" onClick={()=>{setMode('hub');setCard(null)}}>← Retour</button><div className="eyebrow"><Sparkles size={14}/> PALM PLAY</div><h2>{title}</h2><p>{sub}</p>{card?<div className="matchHero"><span>{card[0]}</span><h2>{card[1]}</h2></div>:<div className="resultCard"><div className="rare">PRÊTS ?</div><h2 className="profile">Une carte. Une réaction.</h2><p>Pas de score, pas de bonne réponse. Le but : lancer les discussions et découvrir ce que les autres pensent vraiment.</p></div>}<button className="primary" onClick={()=>randomCard(list)}>{card?'CARTE SUIVANTE':'LANCER LE JEU'} <ChevronRight/></button></section>;
const picker=(title,sub,min,max,next)=><><button className="backLink" onClick={()=>{setMode('hub');setSelected([])}}>← Retour</button><div className="eyebrow"><Users size={14}/> PALM SOCIAL</div><h2>{title}</h2><p>{sub}</p><div className="peoplePicker">{history.map(h=>{const active=selected.includes(h.id);return <button key={h.id} className={'personPick '+(active?'selected':'')} onClick={()=>{if(!active&&selected.length>=max)return;toggle(h.id)}}><div className="avatar">{(h.name||'P')[0].toUpperCase()}</div><div><b>{first(h.name)}</b><span>{h.profile.name}</span></div><i>{active?<Check size={16}/>:<span/>}</i></button>})}</div>{history.length<min&&<div className="emptySocial"><p>Il manque {min-history.length} profil{min-history.length>1?'s':''}.</p><button className="secondary" onClick={onScan}><Camera/> Scanner quelqu’un</button></div>}<button className="primary" disabled={selected.length<min} onClick={()=>setMode(next)}>{selected.length<min?`Choisis ${min} personne${min>1?'s':''}`:next==='matchResult'?'RÉVÉLER LE MATCH':'ANALYSER LE GROUPE'} <ChevronRight/></button></>;
if(mode==='duoGame')return unlocked?game('Duo Challenge','Vérités, prédictions et mini-défis à faire à deux.',DUO):<section><button className="backLink" onClick={()=>setMode('hub')}>← Retour</button>{paywall('Duo Challenge','Des cartes Vérité, Prédiction et Défi pour prolonger chaque Match.')}</section>;if(mode==='groupGame')return unlocked?game('Group Game','Passe le téléphone. Votez, débattez, révélez-vous.',GROUP):<section><button className="backLink" onClick={()=>setMode('hub')}>← Retour</button>{paywall('Group Game','Un jeu de soirée intégré à PALM : votes, vérités et défis pour toute la bande.')}</section>;if(mode==='match')return <section>{picker('Qui veux-tu matcher ?','Choisis 2 personnes. Le score est gratuit ; PALM Unlimited révèle toute la dynamique du duo.',2,2,'matchResult')}</section>;if(mode==='group')return <section>{unlocked?picker('Qui est dans le groupe ?','Choisis de 3 à 6 personnes pour révéler les rôles, les affinités et la dynamique du groupe.',3,6,'groupResult'):<><button className="backLink" onClick={()=>setMode('hub')}>← Retour</button>{paywall('Débloque le mode Groupe','Alchimie, rôles naturels et meilleurs duos de ta bande.')}</>};
if(mode==='matchResult'&&match)return <section className="socialReport"><button className="backLink" onClick={()=>setMode('match')}>← Changer les personnes</button><div className="eyebrow"><Heart size={14}/> VOTRE MATCH</div><div className="matchHero"><span>{match.label}</span><div className="compatScore">{match.score}%</div><h2>{first(people[0].name)} × {first(people[1].name)}</h2><p>{match.hook}</p></div>{unlocked?<><div className="matchChapters">{match.chapters.map((c,i)=><article key={c.title}><b>0{i+1}</b><div><h3>{c.title}</h3><p>{c.text}</p></div></article>)}</div><button className="primary" onClick={()=>{setMode('duoGame');setCard(null)}}>JOUER EN DUO <ChevronRight/></button><button className="secondary" onClick={()=>shareCompatibility(people[0],people[1],match)}>Partager le Match</button><button className="secondary" onClick={()=>downloadShareCard(people[0],people[1],match)}>Partager la carte</button></>:<><div className="lockedPreview"><div><LockKeyhole/><span>Pourquoi ça fonctionne</span></div><div><LockKeyhole/><span>Votre point de friction</span></div><div><LockKeyhole/><span>Votre superpouvoir de duo</span></div><div><LockKeyhole/><span>Duo Challenge</span></div></div>{paywall('Tu connais le score. Maintenant joue.','Rapport complet + Duo Challenge + Groupe + Group Game + scans illimités.')}</>}</section>;
if(mode==='groupResult'&&group)return <section><button className="backLink" onClick={()=>setMode('group')}>← Modifier le groupe</button><div className="eyebrow"><Users size={14}/> GROUP VIBE</div><h2>Votre groupe</h2><div className="groupScore"><span>ALCHIMIE DU GROUPE</span><strong>{group.avg}%</strong></div><div className="groupRoles"><div><span>⚡ DONNE LE TEMPO</span><b>{first(group.leader.name)}</b></div><div><span>🧲 CONNECTE LE GROUPE</span><b>{first(group.connector.name)}</b></div><div><span>✨ SURPREND LE PLUS</span><b>{first(group.wild.name)}</b></div></div><button className="primary" onClick={()=>{setMode('groupGame');setCard(null)}}>LANCER LE GROUP GAME <ChevronRight/></button></section>;
return <section><div className="eyebrow"><Users size={14}/> PALM SOCIAL</div><h2>Scanne. Matche. Joue.</h2><p>PALM ne s’arrête plus au résultat : utilise vos profils pour lancer une vraie expérience à deux ou avec toute la bande.</p><div className="socialChoices"><button onClick={()=>{setSelected([]);setMode('match')}}><Heart/><div><b>Match</b><span>Score gratuit</span><p>Compare 2 personnes et découvre votre compatibilité.</p></div><ChevronRight/></button><button onClick={()=>setMode('duoGame')}><Sparkles/><div><b>Duo Challenge {unlocked?'':'🔒'}</b><span>2 joueurs</span><p>Vérités, prédictions et défis rapides.</p></div><ChevronRight/></button><button onClick={()=>{setSelected([]);setMode('group')}}><Users/><div><b>Groupe {unlocked?'':'🔒'}</b><span>3 à 6 amis</span><p>Alchimie, rôles et meilleurs duos.</p></div><ChevronRight/></button><button onClick={()=>setMode('groupGame')}><Sparkles/><div><b>Group Game {unlocked?'':'🔒'}</b><span>Mode soirée</span><p>Votes, vérités, débats et actions.</p></div><ChevronRight/></button></div><div className="socialProfiles"><div className="socialProfilesHead"><b>Mes personnes</b><span>{history.length} profil{history.length!==1?'s':''}</span></div>{history.map(h=><div className="miniPerson" key={h.id}><button onClick={()=>onOpen(h)}><div className="avatar">{(h.name||'P')[0].toUpperCase()}</div><div><b>{first(h.name)}</b><span>{h.profile.name}</span></div></button><div><button onClick={()=>onRename(h)}>Nom</button><button className="dangerMini" onClick={()=>onDelete(h)}><X size={13}/></button></div></div>)}<button className="addPerson" onClick={onScan}><Camera size={17}/> Scanner quelqu’un</button></div></section>;
}